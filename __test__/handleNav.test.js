import {
    handleNav
} from '../src/client/js/handleNav'


describe('ensure handleNav function exists', () => {
    test('ensure handleNav function exists', () => {
        expect(handleNav).toBeDefined();
    })
})